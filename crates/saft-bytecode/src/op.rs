#[derive(Debug, Clone)]
pub enum Op {
    Pop,
    PopN(usize),
    Return,
    Nil,
    Bool(bool),
    Float(f64),
    Integer(i64),
    String(String),
    Var(usize),
    JmpFalse(usize),
    JmpTrue(usize),
    Jmp(usize),
    Not,
    Add,
    Pow,
    IDiv,
    Div,
    Mul,
    Sub,
    And,
    Or,
    Lt,
    Le,
    Gt,
    Ge,
    Eq,
    Ne,
    /// Pop n + 1 values, then push the last one, used in blocks
    TrailPop(usize),
    Call(usize),
    Index,
    Vec(usize),
    Negate,
    Assign(usize),
    AssignIndexable,
    Constant(usize),
}
